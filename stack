class stack:
    def __init__(self):
        self.stack=list()
    def push(self,data):
        self.stack.append(data)
    def pop(self):
        self.pop()
    def peek(self):
        return self.stack[len(self.stack)-1]
    def empty(self):
        if len(self.stack)==0:
            return True
        else:
            return False
    def show(self):
        print(self.stack)
    def get(self):
        return stack
